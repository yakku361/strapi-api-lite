export enum ArticlePopulate {
  Author = 'author',
  Category = 'category',
  Blocks = 'blocks',
  Cover = 'cover',
}

export enum PagePopulate {
  Dynamic = 'dynamic',
}

export enum ProductPopulate {
  Cover = 'cover',
}
